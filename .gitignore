node_modules/
dist/
*.tsbuildinfo
.DS_Store
Thumbs.db
*-protocoll.txt
*-protocol.txt

# Maintenance run logs - pipeline material, not repository content
_after-care/

# Local secrets and credentials
.env
.env.*
!.env.example
!.env.sample
.npmrc
secrets.json
credentials.json
token.json
tokens.json
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
id_rsa
id_dsa
id_ecdsa
id_ed25519
*recovery*code*.txt
*recovery*codes*.txt

# mcp-publisher login artifacts (credentials, never commit)
.mcpregistry_*

# Multi-host sync conflicts (OneDrive, Syncthing)
*-conflict-*
*.sync-conflict-*
*.sync-temp-*

# Multi-agent locks & lock system
LOCK
LOCK.*
LOCK*.txt
*.lock
!package-lock.json

# Test, coverage & packaging caches
.pytest_cache/
.ruff_cache/
.coverage
coverage/
.vitest/
wheelhouse/
.wheel-smoke/

# Temporary editor & backup files
*.tmp
*.bak
*.swp
*~
